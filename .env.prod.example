# RTS Curator Recommendation System - Production Configuration
# Copy this to .env when deploying to production

# ---- General ----
APP_ENV=prod
APP_LOG_LEVEL=WARNING  # Less verbose in production

# ---- API Server (Production) ----
APP_API_HOST=0.0.0.0  # Listen on all interfaces (Docker/cloud)
APP_API_PORT=8000
APP_API_RELOAD=false  # No auto-reload in production
APP_API_WORKERS=4  # Multiple workers for better performance
APP_CORS_ORIGINS=["https://yourdomain.com"]  # Restrict to your domain

# ---- TMDB API ----
APP_TMDB__API_KEY=your_tmdb_api_key_here
APP_TMDB__API_BASE_URL=https://api.themoviedb.org/3

# ---- Data Paths (production paths) ----
APP_DATA_ROOT=/app/data  # Docker volume mount path
APP_MODELS_DIR=/app/data/models

# ---- Reproducibility ----
APP_RANDOM_SEED=42
